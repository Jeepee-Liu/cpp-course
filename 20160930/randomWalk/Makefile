# This CLASS is Usually used as a package
include ../makefile.config

randomWalk.o : randomWalk.h
	$(CXX) $(STD) $(CFLAG) -c randomWalk.cpp -o ../randomWalk.o

.PHONY : clean
clean :
	rm -rf ../randomWalk.o